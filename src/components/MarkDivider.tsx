/**
 * Decorative seam between sections. Logo intentionally removed —
 * the divider now reserves no visual space.
 */
const MarkDivider = () => {
  return <div aria-hidden="true" className="h-0" />;
};

export default MarkDivider;
